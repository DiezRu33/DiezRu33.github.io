Esta es la historia de leo messi

Era crack, siempre fue crack y seguira siendo crack

aguante chicago bulls

aguante el helado de crema y el chocolate

git log --stat | see all the changes
git show | last change

ssh-keygen -t rsa -b 4096 -C "diegoruffa@hotmail.com" | create SSH keygen
- save on file .ssh/id_rsa.pub;

eval $(ssh-agent -s) | validate ssh-agent;

.git show --stat
git log --list
git status
git clone
git diff
git merge
git branch
git checkout
git add
git commit -em "";